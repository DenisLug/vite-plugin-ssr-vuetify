Example of using `vite-plugin-ssr` with Vue and Vuetify.

To reproduce error:

```bash
git clone https://github.com/DenisLug/vite-plugin-ssr-vuetify.git
cd vite-plugin-ssr-vuetify
npm install
npm run build
```
